// Accept a string from user and print its each character on a new line

function stringPrint(name) {
    for (let i = 0; i < name.length; i++) {
        console.log(name.charAt(i));
    }
}
stringPrint("Tarun");