//Print the message along with values and sum (Ex - The sum of 45 & 12 = 57)
let a = 10;
let b = 20;

// console.log(`The Sum of ${a} & ${b}=> ${a + b}`);

//already done in Q1 .
// sumOF(23, 23); //cannot access before initilizatio..

const sumOF = (val1, val2) => {
    return console.log(`The Sum of ${val1} & ${val2} is ${val1 + val2}`);
}

sumOF(100, 200);
